name: aqua_ad_widget
description: Flutter widget for Revive Adserver integration with image/video ads, auto-refresh, click tracking, and carousel support.
version: 5.0.3
homepage: https://github.com/appfactorysrl/aqua-ad-widget
repository: https://github.com/appfactorysrl/aqua-ad-widget
issue_tracker: https://github.com/appfactorysrl/aqua-ad-widget/issues
documentation: https://github.com/appfactorysrl/aqua-ad-widget#readme


environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

platforms:
  android:
  ios:
  web:
  macos:
  linux:
  windows:

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  url_launcher: ^6.2.4
  video_player: ^2.10.1
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

topics:
  - ads
  - advertising
  - revive
  - adserver
  - flutter-widget